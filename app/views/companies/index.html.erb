<h1>Companies</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>
  <% @companies.each do |company| %>
    <tr>
      <td><%= company.name %></td>
      <td><%= link_to("Sales", company_sales_path(company) )%></td>
    </tr>
  <% end -%>
</table>